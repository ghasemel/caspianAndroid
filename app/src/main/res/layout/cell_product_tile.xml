<?xml version="1.0" encoding="utf-8"?>
<!--
    One dish tile. Tapping ANYWHERE on it adds one. This is the single most
    repeated interaction in the app, so it must never require aiming at a small
    plus control. The badge shows how many are already in the draft.
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="96dp"
                android:layoutDirection="rtl"
                android:background="@drawable/product_tile"
                android:padding="8dp">

    <TextView
        android:id="@+id/productTileName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:maxLines="2"
        android:ellipsize="end"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="#212121" />

    <TextView
        android:id="@+id/productTilePrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:typeface="sans"
        android:textSize="14sp"
        android:textColor="#546e7a" />

    <TextView
        android:id="@+id/productTileBadge"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:background="@drawable/qty_badge"
        android:gravity="center"
        android:typeface="sans"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="#ffffff"
        android:visibility="gone" />

</RelativeLayout>
