<?xml version="1.0" encoding="utf-8"?>
<!-- Exact quantity + kitchen note. Reached by long-pressing a dish tile. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:layoutDirection="rtl"
              android:padding="16dp">

    <TextView
        android:id="@+id/dialogLineTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#212121" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/restaurant_line_count"
        android:textSize="14sp" />

    <EditText
        android:id="@+id/dialogLineCount"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:inputType="numberDecimal"
        android:typeface="sans"
        android:gravity="center"
        android:textSize="20sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/restaurant_line_spec"
        android:textSize="14sp" />

    <EditText
        android:id="@+id/dialogLineSpec"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:inputType="text"
        android:gravity="start"
        android:textSize="16sp" />

</LinearLayout>
